[build]
  command   = "git clone https://github.com/flutter/flutter.git --branch stable --depth 1 ~/flutter && ~/flutter/bin/flutter build web --release"
  publish   = "build/web"

[build.environment]
  PATH = "/opt/build/repo:$HOME/flutter/bin:$PATH"
